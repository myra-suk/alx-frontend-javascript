/* Implement a class named "Classroom" with the following properties:
 * An attribute named maxStudentsSize (Number) and assigned to _maxStudentsSize
 */

/* eslint-disable no-underscore-dangle */

export default class ClassRoom {
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
